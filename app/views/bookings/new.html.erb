<%= link_to "retour", flats_path, class:"btn btn-primary" %>

<div class="form">
  <div class="title-form" >
    <h1>Make your Book📅</h1>
  </div>
  <div class="form-input">
    <%= simple_form_for [@flat, @booking], url: flat_bookings_path, method: :post do |f| %>
              <%= f.input :check_in, html5: true, label: "Check-in" %>
              <%= f.input :check_out, html5: true, label: "Checkout" %>
              <%= f.submit "Confirm", class:"button-black-yellow" %>
    <% end %>
  </div>
</div>
